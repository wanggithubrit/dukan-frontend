const fs=require('fs');const parser=require('@babel/parser');const traverse=require('@babel/traverse').default;const code=fs.readFileSync('app/shop/home.js','utf8');const ast=parser.parse(code,{sourceType:'module',plugins:['jsx']});const bad=[];traverse(ast,{JSXText(path){const v=path.node.value;if(!v.trim())return;let cur=path.parentPath;let inText=false;while(cur){if(cur.type==='JSXElement' && cur.node.openingElement && cur.node.openingElement.name){const n=cur.node.openingElement.name; if(n.type==='JSXIdentifier' && n.name==='Text'){inText=true;break;} } cur=cur.parentPath;} if(!inText){bad.push({text:v.trim(),line:path.node.loc.start.line});}}});console.log(JSON.stringify(bad,null,2));